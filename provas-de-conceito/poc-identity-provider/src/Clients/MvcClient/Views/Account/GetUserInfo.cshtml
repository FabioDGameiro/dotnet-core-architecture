@model System.Collections.Generic.IEnumerable<System.Security.Claims.Claim>
@{
    ViewData["Title"] = "GetUserInfo";
}
<h1>User Info Claims</h1>
<dl>
   @foreach (var claim in Model)
   {
      <dt>@claim.Type</dt>
      <dd>@claim.Value</dd>
   }
</dl>